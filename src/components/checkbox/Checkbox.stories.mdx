import { Meta, Story, Preview, ArgsTable } from "@storybook/addon-docs/blocks";
import Checkbox from "./Checkbox";
import { Stack } from "@chakra-ui/react";

<Meta title="Forms/Checkbox" component={Checkbox} />

## Checkbox

For most cases, our default buttons with the standard defined colorschemes will suffice.

<Preview>
  <Story name="All">
    <Stack>
      <Checkbox>Checkbox</Checkbox>
      <Checkbox defaultChecked>Default checked</Checkbox>
      <Checkbox isDisabled>Disabled</Checkbox>
      <Checkbox colorScheme="orange" isChecked={true}>
        Checked Only and New Color
      </Checkbox>
      <Checkbox isInvalid isReadOnly>
        Invalid and ReadOnly
      </Checkbox>
    </Stack>
  </Story>
</Preview>

<ArgsTable of={Checkbox} />
